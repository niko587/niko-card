# Niko Stathis card page

Mobile landing page for the NFC/QR business card. `index.html` is the whole site; `source/` holds the Blender script, the exported 3D models and the build script that rebuilds `index.html`.
